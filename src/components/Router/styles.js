import styled from "styled-components";
import Loader from "react-loader-spinner";

export const LoaderContainer = styled(Loader)`
  position: absolute;
  top: 50%;
  left: 50%;
  margin: -50px 0 0 -50px;
`;
